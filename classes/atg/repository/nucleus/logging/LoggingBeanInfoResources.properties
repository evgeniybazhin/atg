# atg.nucleus.logging  LoggingBeanInfoResources.properties
#
# Automatically generated by BeanInfoGenerator Wed Jun 27 22:27:08 EDT 2007.

# ============================================================================================================
#
# The following resources SHOULD be converted when performing localization with the following exceptions
# Any files ending with .gif, .html or .jhtml  (unless these are to be changed to locale specific files)
# Any numeric values such as 200,200. These represent window dimensions which will typically remain the same when localized.
#
# ============================================================================================================

# These resources should be localized.
atg_flags=i18n


#
# RotatingFileLogger.
#
RotatingFileLogger.shortDescription=A log listener that writes log events to files and rotates the files periodically.
#
# Methods.
RotatingFileLogger.getScheduler.shortDescription=The scheduler used to schedule rotations
RotatingFileLogger.getSchedule.shortDescription=The schedule on which rotations are managed
RotatingFileLogger.getLogArchivePath.shortDescription=The path used to store rotated log archives
RotatingFileLogger.getMaximumArchiveCount.shortDescription=The maximum number of archives files that will be kept
RotatingFileLogger.isArchiveCompressed.shortDescription=True if archived log files are compressed when rotated
RotatingFileLogger.performScheduledTask.param0.shortDescription=calling the job
RotatingFileLogger.performScheduledTask.param1.shortDescription=the ScheduledJob
RotatingFileLogger.rotateLogs.shortDescription=Rotates the log files
RotatingFileLogger.rotateUncompressedLogs.shortDescription=Rotates the uncompressed log files
RotatingFileLogger.rotateCompressedLogs.shortDescription=Rotates the compressed log files
#
# Properties.
RotatingFileLogger.maximumArchiveCount.shortDescription=The maximum number of archives files that will be kept
RotatingFileLogger.logArchivePath.shortDescription=The path used to store rotated log archives
RotatingFileLogger.scheduler.shortDescription=The scheduler used to schedule rotations
RotatingFileLogger.archiveCompressed.shortDescription=True if archived log files are compressed when rotated
RotatingFileLogger.schedule.shortDescription=The schedule on which rotations are managed


#
# LogListenerQueue.
#
LogListenerQueue.shortDescription=A queuing version of a log listener
LogListenerQueue.icon=/atg/ui/common/images/loglistenercomp.gif
#
# Events.
LogListenerQueue.log.shortDescription=Logging event
#
# Methods.
LogListenerQueue.addLogListener.shortDescription=Logging event
LogListenerQueue.getLogListenerCount.shortDescription=Number of logging event listeners
LogListenerQueue.getLogListeners.shortDescription=List of logging event listeners
#
# Properties.
LogListenerQueue.logListeners.shortDescription=List of logging event listeners
LogListenerQueue.logListenerCount.shortDescription=Number of logging event listeners


#
# FileLogger.
#
FileLogger.shortDescription=A log listener that writes logged events into a file
#
# Methods.
FileLogger.setLogFileName.param0.shortDescription=the log file name
FileLogger.setLogFileName.shortDescription=The name of the log file
FileLogger.setLogFilePath.param0.shortDescription=the log file path
FileLogger.setLogFilePath.shortDescription=The path to the file (without the filename)
FileLogger.open.shortDescription=Opens the log file
FileLogger.close.shortDescription=Closes the log file
FileLogger.flush.shortDescription=Flushes the log file data to disk
#
# Properties.
FileLogger.logFilePath.shortDescription=The path to the file (without the filename)
FileLogger.logFileName.shortDescription=The name of the log file


#
# EmailLogger.
#
EmailLogger.shortDescription=A log listener that sends logged events via e-mail
#
# Events.
EmailLogger.email.shortDescription=Event generated whenever e-mail is sent
#
# Methods.
EmailLogger.getScheduler.shortDescription=The scheduler used to trigger e-mail messages
EmailLogger.getSchedule.shortDescription=The schedule on which e-mail logs are generated
EmailLogger.getDefaultRecipients.shortDescription=The default list of recipients for the e-mail
EmailLogger.getDefaultFrom.shortDescription=The default sender (From:) field
EmailLogger.getDefaultSubject.shortDescription=The default subject field
EmailLogger.getDefaultExtraHeaders.shortDescription=Extra headers to include in the e-mail by default
EmailLogger.getDefaultBody.shortDescription=Message body to prepend to all e-mail messages
EmailLogger.getLogEventThreshold.shortDescription=Number of events that should be queued before an e-mail is sent
EmailLogger.addEmailListener.shortDescription=Event generated whenever e-mail is sent
EmailLogger.getEmailListenerCount.shortDescription=Number of e-mail event listeners
EmailLogger.flushLogEventQueue.shortDescription=Sends all queued log events as an e-mail message
EmailLogger.logEvent.param0.shortDescription=the log event
#
# Properties.
EmailLogger.defaultSubject.shortDescription=The default subject field
EmailLogger.defaultRecipients.shortDescription=The default list of recipients for the e-mail
EmailLogger.defaultBody.shortDescription=Message body to prepend to all e-mail messages
EmailLogger.logEventThreshold.shortDescription=Number of events that should be queued before an e-mail is sent
EmailLogger.scheduler.shortDescription=The scheduler used to trigger e-mail messages
EmailLogger.schedule.shortDescription=The schedule on which e-mail logs are generated
EmailLogger.defaultFrom.shortDescription=The default sender (From:) field
EmailLogger.defaultExtraHeaders.shortDescription=Extra headers to include in the e-mail by default
EmailLogger.emailListenerCount.shortDescription=Number of e-mail event listeners


#
# DispatchLogger.
#
DispatchLogger.shortDescription=A log listener that sorts log events by type and dispatches them to other log listeners
DispatchLogger.icon=/atg/ui/common/images/loglistenercomp.gif
#
# Methods.
DispatchLogger.setLogEvents.shortDescription=List of log event class names to use for sorting
DispatchLogger.setLogDestinations.shortDescription=List of log listeners to dispatch events to
DispatchLogger.getDefaultDestinations.shortDescription=Listeners to dispatch events to when they don't match specified classes
DispatchLogger.logEvent.param0.shortDescription=the log event
#
# Properties.
DispatchLogger.logDestinations.shortDescription=List of log listeners to dispatch events to
DispatchLogger.logEvents.shortDescription=List of log event class names to use for sorting
DispatchLogger.defaultDestinations.shortDescription=Listeners to dispatch events to when they don't match specified classes


#
# DisplayLogger.
#
DisplayLogger.shortDescription=A logger which writes to standard I/O devices
DisplayLogger.icon=/atg/ui/common/images/loglistenercomp.gif
#
# Methods.
DisplayLogger.setCropStackTrace.shortDescription=Flag to determine whether stack traces should be cropped
DisplayLogger.setMaxLinesInStackTrace.shortDescription=Maximum number of lines to show in a stack trace
DisplayLogger.setSuppressTimestamp.shortDescription=True if each message should only be prefixed with an originator name. Timestamp , date and identifier will not be displayed.
DisplayLogger.setPrefixEachMessage.shortDescription=True if each message should be prefixed with information from the prefix stream
DisplayLogger.setPrefixEachLine.shortDescription=True if each line should be prefixed with information from the prefix stream
DisplayLogger.getWrittenEventCount.shortDescription=The number of log events written
DisplayLogger.setLoggingEnabled.shortDescription=Set this to false to disable logging.
DisplayLogger.logEvent.param0.shortDescription=the log event
DisplayLogger.writeLogEvent.param0.shortDescription=the log event
#
# Properties.
DisplayLogger.suppressTimestamp.shortDescription=True if each message should only be prefixed with an originator name. Timestamp , date and identifier will not be displayed.
DisplayLogger.prefixEachLine.shortDescription=True if each line should be prefixed with information from the prefix stream
DisplayLogger.loggingEnabled.shortDescription=Set this to false to disable logging.
DisplayLogger.prefixEachMessage.shortDescription=True if each message should be prefixed with information from the prefix stream
DisplayLogger.maxLinesInStackTrace.shortDescription=Maximum number of lines to show in a stack trace
DisplayLogger.cropStackTrace.shortDescription=Flag to determine whether stack traces should be cropped
DisplayLogger.writtenEventCount.shortDescription=The number of log events written


#
# PrintStreamLogger.
#
PrintStreamLogger.shortDescription=A log listener that writes logged events to a print stream
#
# Methods.
PrintStreamLogger.setPrintStream.param0.shortDescription=the PrintStream
PrintStreamLogger.setPrintStream.shortDescription=PrintStream to write log events to
#
# Properties.
PrintStreamLogger.printStream.shortDescription=PrintStream to write log events to
